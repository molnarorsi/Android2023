package fragments

import android.app.Application

//class App : Application() {
//    private val database by lazy {RecipeDatabase.getDatabase(this)}
//    val repository by lazy { repository.recipe.RecipeRepository(database.recipeDao()) }
//}